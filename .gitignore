# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg

# Virtual environments
venv/
env/
.venv/

# Streamlit
.streamlit/secrets.toml

# Local data / detections (not needed on cloud)
saved_detections/
users.db

# YOLO training runs (large files — keep only weights)
runs/detect/*/
!runs/detect/train_extended/
!runs/detect/train_extended/weights/
!runs/detect/train_extended/weights/best.pt

# Dataset caches
train/labels.cache
valid/labels.cache
test/labels.cache

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
